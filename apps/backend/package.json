{
    "name": "backend",
    "private": true,
    "scripts": {
      "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
      "build": "echo 'No build step for FastAPI'",
      "lint": "echo 'Add backend linting later'"
    }
  }
  